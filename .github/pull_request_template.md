<!-- Describe what lands and why. Delete the sections that do not apply. -->

## What lands

## Gates

<!-- Name each gate you ran and its result. A gate you did not run is not a
     gate that passed - say so rather than leaving it unmentioned. -->

## Issues

<!-- Write one closing keyword per issue, each on its own line:

       Closes #123
       Closes #124

     A comma-separated list does NOT work. GitHub binds the keyword to the
     first number only, so `Closes #123, #124, #125` closes #123 and silently
     leaves the rest open - the fixes merge, the issues stay red, and nobody
     notices until someone triages them by hand. This has already happened
     here: one PR listed six issues that way and closed one.

     Use `Refs #123` for an issue the change touches but does not close. -->
