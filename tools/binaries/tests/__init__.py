"""Guards for the release-binary builder in :mod:`tools.binaries`.

These sit inside the tree they exercise, the way every other test package in
this repository does, so the builder is covered by the same convention as the
code it operates on rather than by nothing at all.
"""

from __future__ import annotations
