---
tags:
  - '#exec'
  - '#provider-mcp-enrollment'
date: '2026-07-16'
modified: '2026-07-22'
step_id: 'S63'
related:
  - "[[2026-07-15-provider-mcp-enrollment-plan]]"
---

# Repeat every platform-aware release gate from zero after the real auto-delegation isolation correction and stop on the first failure

## Scope

- Clean candidate commit `46eca748447eace8cbb70fce539d3e58e6b59e5a`.
- Locked Windows and fresh exact-archive POSIX environments.
- Unique Windows and POSIX `M`, `P`, `J`, and `F` collection ledgers.
- Real auto-delegation precedence, fallback, search, and index coverage.
- Preserved S54 and S56 corrective contracts.
- Complete stop-on-first-failure release sequence.

## Description

- Verify the clean candidate, exact 1,117-document corpus, complete diff, locked
  dependency graph, and scikit-learn wheel payload.
- Recollect and reconcile the complete Windows and POSIX test ledgers.
- Execute the real POSIX FIFO capability test.
- Re-run the fresh-interpreter auto-delegation cases beside the pre-existing
  installed service on port `55108`.
- Review the S62 correction independently before starting runtime execution.
- Stop when the review identifies missing real index-command delegation coverage.

## Outcome

Failed release readiness. The environment, corpus, diff, collection,
set-reconciliation, FIFO, and focused fresh-interpreter preflight gates passed.
The independent review then found that `TestAutoDelegation` resolves both search
and index ports but invokes only the search command. It does not exercise
`_index.index`, `_try_service_delegation`, or the reindex request payload under
either machine-global authority or status-file fallback.

This is the first S63 target red. The Windows and POSIX runtime campaigns,
static and type checks, package builds, public Core resolution, installed
Claude and Codex recognition, idempotence, unenrollment, uninstall, approval,
merge, tag, publication, and release gates were not started. They receive no
credit or waiver.

## Notes

- The focused two-case class passed while the pre-existing service process on
  port `55108` retained the same process identifier and start time. Its
  `service.json` was absent, so no byte-preservation claim is made.
- The Windows ledger was 2,271 total and unique, 1,828 `M`, six disjoint `P`,
  13 Windows-only `J`, no `F`, and a 1,834-item campaign.
- The POSIX ledger was 2,259 total and unique, 1,829 `M`, six disjoint `P`, no
  `J`, one real FIFO `F` inside `M`, and a 1,835-item campaign.
- No additional S54 or S56 regression attributable to the S62 diff was found.
- Remediation requires real-loopback, fresh-interpreter index CLI probes under
  both discovery modes, asserting the selected endpoint and request payload,
  including `initiator_kind="cli"`, without test doubles.
- No production, test, dependency, or lock file changed during S63.
